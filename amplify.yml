version: 1
frontend:
  phases:
    preBuild:
      commands:
        - mkdir -p dist
        - cp landing-radar-instagram.html dist/index.html
        - cp protocolo-de-performance.html dist/protocolo-de-performance.html
        - cp signal.html dist/signal.html
    build:
      commands:
        - echo "Static build complete"
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths: []
