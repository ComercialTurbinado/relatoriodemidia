version: 1
frontend:
  phases:
    preBuild:
      commands:
        - mkdir -p dist
        - cp landing-radar-instagram.html dist/index.html
    build:
      commands:
        - echo "Static build complete"
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths: []
